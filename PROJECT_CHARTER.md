# 업무수첩 프로젝트 헌장

## 제품 정체성

업무수첩은 단순한 음성 업무기록 앱이 아니다.

> **업무수첩은 개인 업무에 필요한 여러 독립 도구를 한곳에서 실행하고, 필요할 때 업무기록·일정·CRM·브리핑으로 연결하는 개인 업무 통합 앱이다.**

빠르게 말하고 확실히 기록하는 기능은 계속 핵심 기능으로 유지하지만, 제품의 장기 범위는 업무기록 하나에 한정하지 않는다.

## 장기 제품 범위

업무수첩은 다음과 같은 도구를 하나의 App Shell에서 제공하는 방향으로 확장한다.

- 말해서 업무기록
- 일정 및 후속조치
- 문서 스캔 / PDF
- 통화녹음 정리
- 회의·미팅 녹음 정리
- 메일 확인·업무화
- 카카오톡/문자 캡처 분석
- 고객 / CRM
- 업무 문서
- 오늘 할 일 / 브리핑

이 목록은 고정 기능 목록이 아니라 제품 방향의 예시다. 새 기능은 업무에 실제로 필요한지와 독립 도구로서 가치가 있는지를 먼저 본다.

## 최상위 제품 원칙

### 1. 각 기능은 단독으로도 쓸 수 있어야 한다

새 기능은 가능한 한 다음 흐름만으로도 독립적인 사용가치를 가져야 한다.

`입력 → 자체 처리 → 자체 결과 → 저장/공유`

업무수첩의 다른 기능과 연결해야만 쓸 수 있는 구조를 기본값으로 만들지 않는다.

예:

- 스캐너: 사진/갤러리 → 보정 → 여러 장 → PDF → 저장/공유
- 통화정리: 녹음 선택 → 녹취 → 통화 보고서 → 저장/공유
- 회의정리: 녹음 → 녹취 → 회의록 → 저장/공유

### 2. 업무수첩은 독립 도구를 연결하는 허브다

독립 기능의 핵심 사용 흐름이 완성된 뒤에만 다음 연결 기능을 붙인다.

- 업무기록으로 보내기
- 일정 만들기
- 후속조치 만들기
- CRM 연결
- 브리핑에 반영

App Shell은 모든 기능의 세부 로직을 직접 소유하는 거대한 모듈이 아니라, 독립 모듈을 찾고 실행하고 연결하는 얇은 허브를 지향한다.

### 3. 외부 서비스는 제품의 내부 데이터 계약을 지배하지 않는다

통화, 스캔, 회의 등 도메인 모듈 안에서 Notion, Supabase, 특정 STT/AI 공급자, Kakao 등의 외부 스키마를 직접 기준으로 삼지 않는다.

모듈은 업무수첩 내부 표준 데이터 계약을 먼저 사용하고, 외부 시스템 연결은 Adapter / Service 경계를 통해 수행한다.

이 원칙의 목적은 공급자 교체, 저장소 변경, PWA→네이티브 확장 때 도메인 기능을 다시 작성하지 않게 하는 것이다.

### 4. 정상 동작 중인 기능은 모듈화를 이유로 대규모 재작성하지 않는다

모듈화는 점진적으로 적용한다.

- 새 기능은 처음부터 모듈 원칙을 적용한다.
- 기존 기능은 수정하거나 확장할 때 안전한 범위에서 경계를 개선한다.
- 표준 준수 자체를 목적으로 정상 기능을 한 번에 갈아엎지 않는다.

### 5. 사람의 최종 기록과 시스템 내부 상태를 구분한다

현재 기준 역할은 다음과 같다.

| 계층 | 역할 |
| --- | --- |
| 휴대폰/사용자 기기 | 원본 음성·사진 등 사용자가 가진 원본의 기본 보관 위치 |
| Notion | 사람이 읽고 관리하는 실제 업무기록의 현재 기본 원장 |
| 업무수첩 내부 계약 | 모듈 사이에서 공유하는 제품 표준 데이터 형태 |
| Supabase 등 앱 DB | 향후 처리상태, 관계, 재시도, 사용량, 시스템 메타데이터를 저장하는 내부 엔진 후보 |
| Netlify | PWA 배포, API 실행, 예약 실행, 외부 시스템 중계 |
| STT / AI | 음성·텍스트를 처리하는 교체 가능한 Processor / Adapter |
| Kakao / Calendar 등 | 결과를 전달하거나 외부 업무도구와 연결하는 Adapter |

Notion은 현재 실제 업무기록의 기준을 유지한다. 다만 개별 모듈의 내부 구현이 Notion 속성명이나 데이터베이스 스키마에 직접 종속되어서는 안 된다.

Supabase나 다른 데이터베이스를 실제 업무 원장으로 변경하는 결정은 별도 명시적 제품 결정 없이 자동으로 하지 않는다.

## 개인정보·비용 원칙

- 원본 음성·사진은 필요한 처리를 위해 명시적으로 선택하기 전까지 기기를 기본 원본 위치로 본다.
- 서버 저장이 필요하면 목적과 보관기간을 최소화하고 삭제 정책을 둔다.
- STT, AI, OCR 등 사용량 과금 기능은 비용 게이트와 사용량 기록을 둔다.
- 공급자 선택이나 유료 기능 도입은 모듈의 핵심 도메인 로직과 분리한다.

## PWA와 네이티브 방향

PWA를 우선 개발기반으로 유지한다.

웹에서 충분히 구현 가능한 기능은 PWA에서 완성한다. 카메라, 파일, 공유, 기기 OCR, 통화 접근처럼 네이티브 기능의 비중이 커져 명확한 제품 이득이 있을 때 현재 모듈 구조를 유지한 채 Android 앱 껍데기 또는 네이티브 브리지를 검토한다.

네이티브 전환 자체를 목적으로 기존 웹 기능을 다시 작성하지 않는다.

## 제품 개발 우선순위

1. 기록과 원본 보존의 신뢰성
2. 실제 업무에서 반복 사용 가능한 독립 기능 완성도
3. 입력 및 처리 속도
4. 모바일 조작성
5. 개인정보·보안·비용 통제
6. 업무기록·일정·CRM 등 모듈 간 연결
7. 자동분류·AI 정확도와 고급 자동화

## 문서의 역할

- 이 문서: 제품 정체성, 장기 방향, 변하지 않아야 할 프로젝트 원칙
- `docs/MODULE_ARCHITECTURE_V1.md`: 독립 모듈, App Shell, Adapter, 데이터 소유권의 상세 기준
- `AGENTS.md`: 일상 개발에서 매번 확인할 압축 실행 규칙
- GitHub Issue / PR: 현재 작업 범위와 실제 구현·검증 상태

제품 정체성이나 위 원칙을 바꾸는 변경은 일반 기능 개발과 구분해 명시적으로 검토한다.
