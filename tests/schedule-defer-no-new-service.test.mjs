import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const files=['../netlify/functions/schedule-defer.mts','../netlify/functions/schedule-advance-push.mts'];test('신규 기능은 기존 Supabase/Netlify/Web Push 외 서비스 의존을 추가하지 않는다',()=>{for(const f of files){const s=fs.readFileSync(new URL(f,import.meta.url),'utf8');assert.doesNotMatch(s,/axios|firebase|onesignal|pusher|twilio/i);}});
