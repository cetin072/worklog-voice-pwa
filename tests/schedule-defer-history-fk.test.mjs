import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const sql=fs.readFileSync(new URL('../supabase/migrations/20260917190000_schedule_defer_reminder_v1.sql',import.meta.url),'utf8');test('미루기 이력은 workspace/schedule FK를 가진다',()=>{assert.match(sql,/workspace_id uuid not null references public\.workspaces/);assert.match(sql,/schedule_id uuid not null references public\.schedules/);});
