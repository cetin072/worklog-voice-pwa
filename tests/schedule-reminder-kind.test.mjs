import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const sql=fs.readFileSync(new URL('../supabase/migrations/20260917190500_schedule_advance_notification_v1.sql',import.meta.url),'utf8');test('delivery kind는 기존 morning/afternoon과 신규 schedule_advance를 모두 허용한다',()=>{for(const kind of ['morning_digest','afternoon_incomplete','schedule_advance'])assert.match(sql,new RegExp(kind));});
