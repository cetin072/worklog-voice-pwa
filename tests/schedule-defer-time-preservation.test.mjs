import test from 'node:test';import assert from 'node:assert/strict';import {deferScheduleTime} from '../netlify/shared/schedule-defer-reminder.mjs';for(const preset of ['day','week','fortnight','month'])test(`${preset} 미루기는 시분초를 보존한다`,()=>{const before=new Date('2026-09-17T05:23:45Z'),after=new Date(deferScheduleTime(before,preset));assert.equal(after.getUTCHours(),before.getUTCHours());assert.equal(after.getUTCMinutes(),before.getUTCMinutes());assert.equal(after.getUTCSeconds(),before.getUTCSeconds());});
