import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const sql=fs.readFileSync(new URL('../supabase/migrations/20260917190000_schedule_defer_reminder_v1.sql',import.meta.url),'utf8');test('미루기 이력 insert와 schedule update는 하나의 DB 함수 안에서 수행된다',()=>{assert.match(sql,/insert into public\.schedule_defer_history/);assert.match(sql,/update public\.schedules/);assert.match(sql,/language plpgsql/);});
