import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const sql=fs.readFileSync(new URL('../supabase/migrations/20260917190000_schedule_defer_reminder_v1.sql',import.meta.url),'utf8');test('DB preset allowlist도 day/week/fortnight/month/custom만 허용한다',()=>assert.match(sql,/p_preset not in \('day','week','fortnight','month','custom'\)/));
