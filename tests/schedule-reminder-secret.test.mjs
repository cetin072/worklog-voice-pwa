import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const sql=fs.readFileSync(new URL('../supabase/migrations/20260917190500_schedule_advance_notification_v1.sql',import.meta.url),'utf8');test('일정 reminder claim은 authenticated/public 직접 실행을 막고 anon scheduler 경계만 연다',()=>{assert.match(sql,/revoke all .* from public,authenticated/);assert.match(sql,/grant execute .* to anon/);});
