import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const sql=fs.readFileSync(new URL('../supabase/migrations/20260917190000_schedule_defer_reminder_v1.sql',import.meta.url),'utf8');test('종일 일정도 날짜 미루기는 가능하지만 시각 reminder는 null이다',()=>{assert.doesNotMatch(sql,/if v\.all_day.*raise/);assert.match(sql,/v\.all_day then null/);});
