import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const js=fs.readFileSync(new URL('../netlify/shared/notification-scheduler.mjs',import.meta.url),'utf8'),sql=fs.readFileSync(new URL('../supabase/migrations/20260917190500_schedule_advance_notification_v1.sql',import.meta.url),'utf8');test('scheduler client와 migration RPC 이름이 일치한다',()=>{const name='claim_schedule_advance_notification_deliveries';assert.match(js,new RegExp(name));assert.match(sql,new RegExp(name));});
