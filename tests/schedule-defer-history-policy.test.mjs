import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const sql=fs.readFileSync(new URL('../supabase/migrations/20260917190000_schedule_defer_reminder_v1.sql',import.meta.url),'utf8');test('클라이언트용 이력 정책은 select만 정의한다',()=>{assert.match(sql,/for select to authenticated/);assert.doesNotMatch(sql,/for insert to authenticated|for update to authenticated|for delete to authenticated/);});
