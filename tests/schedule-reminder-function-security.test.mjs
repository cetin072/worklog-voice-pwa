import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const sql=fs.readFileSync(new URL('../supabase/migrations/20260917190500_schedule_advance_notification_v1.sql',import.meta.url),'utf8');test('scheduler claim은 security definer지만 scheduler secret 검증을 먼저 한다',()=>{assert.match(sql,/security definer/);assert.ok(sql.indexOf('p_scheduler_secret is distinct')<sql.indexOf('return query'));});
