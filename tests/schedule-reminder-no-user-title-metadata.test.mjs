import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const sql=fs.readFileSync(new URL('../supabase/migrations/20260917190500_schedule_advance_notification_v1.sql',import.meta.url),'utf8');test('notification delivery metadata JSON에는 사용자 일정 제목을 넣지 않는다',()=>{const call=sql.match(/jsonb_build_object\([^)]*\)/)?.[0]||'';assert.equal(call,"jsonb_build_object('scheduleId',due.id)");});
