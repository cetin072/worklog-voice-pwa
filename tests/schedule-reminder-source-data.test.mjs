import test from 'node:test';import assert from 'node:assert/strict';import {buildScheduleReminderPayload} from '../netlify/shared/schedule-reminder-content.mjs';test('Push payload에는 location/description 같은 추가 개인정보를 싣지 않는다',()=>{const p=buildScheduleReminderPayload({scheduleId:'s',title:'회의',location:'비밀장소',description:'비밀'});assert.equal(JSON.stringify(p).includes('비밀'),false);});
