import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const sql=fs.readFileSync(new URL('../supabase/migrations/20260917190000_schedule_defer_reminder_v1.sql',import.meta.url),'utf8');const table=sql.slice(sql.indexOf('create table'),sql.indexOf('alter table'));test('미루기 이력 테이블은 일정 제목/설명을 복제 저장하지 않는다',()=>assert.doesNotMatch(table,/\btitle\b|description/));
