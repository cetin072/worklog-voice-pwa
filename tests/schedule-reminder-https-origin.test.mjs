import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const src=fs.readFileSync(new URL('../netlify/shared/notification-scheduler.mjs',import.meta.url),'utf8');test('scheduler app origin은 https만 허용한다',()=>assert.match(src,/url\.protocol === 'https:'/));
