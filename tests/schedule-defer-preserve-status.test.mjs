import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const sql=fs.readFileSync(new URL('../supabase/migrations/20260917190000_schedule_defer_reminder_v1.sql',import.meta.url),'utf8');test('미루기 update는 schedule status를 변경하지 않는다',()=>{const u=sql.slice(sql.indexOf('update public.schedules'),sql.indexOf('return query'));assert.doesNotMatch(u,/status\s*=/);});
