import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const sql=fs.readFileSync(new URL('../supabase/migrations/20260917190000_schedule_defer_reminder_v1.sql',import.meta.url),'utf8');test('미루기는 created_by_user_id를 변경하지 않는다',()=>{const update=sql.slice(sql.indexOf('update public.schedules'),sql.indexOf('return query'));assert.doesNotMatch(update,/created_by_user_id\s*=/);});
