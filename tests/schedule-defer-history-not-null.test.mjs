import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const sql=fs.readFileSync(new URL('../supabase/migrations/20260917190000_schedule_defer_reminder_v1.sql',import.meta.url),'utf8');test('이전/새 시작시각과 actor/preset은 null 불가다',()=>{for(const field of ['changed_by_user_id uuid not null','previous_starts_at timestamptz not null','new_starts_at timestamptz not null','preset text not null'])assert.match(sql,new RegExp(field));});
