import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const api=fs.readFileSync(new URL('../netlify/functions/schedule-defer.mts',import.meta.url),'utf8'),sql=fs.readFileSync(new URL('../supabase/migrations/20260917190000_schedule_defer_reminder_v1.sql',import.meta.url),'utf8');test('미루기 API와 migration RPC 이름이 일치한다',()=>{const name='defer_my_schedule';assert.match(api,new RegExp(name));assert.match(sql,new RegExp(name));});
