# Changelog

업무수첩에서 실제 사용자가 체감하는 제품 변경을 기록합니다.

## 2026-09-16

### 추가
- 홈 상단에 `오늘의 브리핑` 요약 영역을 추가했습니다.
- 오늘 일정, 기한 경과, 후속조치 필요, 다음 일정을 한 화면에서 확인할 수 있습니다.
- 전체 브리핑은 `전체 보기`를 눌러 기존 상세 브리핑으로 이어지도록 했습니다.
- 하단 음성 기록 CTA 주변에 `Voice Quick Dock`을 추가했습니다.
- 음성 기록 중 위쪽 원형 상태 표시에서 녹음 시간을 `00:00` 형식으로 확인할 수 있습니다.
- 음성 CTA 왼쪽에는 `✕ 취소`, 오른쪽에는 `✏️ 메모` 빠른 버튼을 배치했습니다. 메모는 기존 직접 입력으로 이동해 바로 키보드 입력을 시작합니다.
- 앱 시작 로딩 구간에 기존 앱 아이콘, `업무수첩`, `말하면 기록되고, 일정까지 한눈에`를 보여주는 브랜드 시작 화면을 추가했습니다.

### 개선
- 핵심 음성 기록 버튼을 화면 하단 중앙의 엄지 도달 영역으로 이동했습니다.
- 음성 기록 시작과 종료를 같은 위치에서 수행하도록 했습니다.
- 기존 Briefing V2 결과를 재사용해 홈 요약이 별도 데이터 소스를 만들지 않도록 했습니다.
- Voice Quick Dock의 보조 버튼은 기존 `입력 지우기`와 `직접 입력` 기능을 그대로 재사용해 저장·Data Core 로직을 중복하지 않도록 했습니다.
- 왼손·오른손 모두 한 손으로 접근하기 쉽도록 보조 버튼을 중앙 CTA의 좌우에 대칭 배치했습니다.
- 브랜드 시작 화면은 기존 환영 카드와 앱 아이콘을 재사용하고, 별도 API·외부 폰트·추가 이미지를 만들지 않도록 했습니다.
- 빠른 로컬 세션에서도 브랜드 화면이 첫 paint 전에 사라지지 않도록 수정했습니다. 앱 초기화는 뒤에서 계속 진행하면서 시작 화면은 1초 동안 확실히 보이도록 합니다.
- 시작 화면의 아이콘을 더 크게 만들고 수첩 제본을 연상시키는 프레임을 추가했습니다. `업무수첩`과 메인 문구도 확대하고, `말하면 기록되고,` / `일정까지 한눈에` 두 줄로 나눠 화면 정중앙을 크게 차지하도록 조정했습니다.
- 설치 PWA가 먼저 보여주는 운영체제 시작 화면과 앱 내부 브랜드 화면을 흰 배경 + 같은 수첩·마이크 아이콘으로 통일해 서로 다른 이미지가 두 번 나오는 느낌을 줄였습니다.
- 브리핑의 `완료`를 누르면 서버 응답과 전체 재조회를 기다리지 않고 해당 업무와 집계가 화면에서 즉시 반영되도록 개선했습니다. 서버 저장 실패 시에는 화면을 원래 상태로 되돌립니다.
- 완료 저장 성공 뒤 전체 브리핑 재확인은 사용자 입력을 막지 않는 백그라운드 동기화로 바꿨습니다.
- 완료 후 `실행 취소`는 화면을 따라다니는 위치를 유지하면서도, 하단 음성 버튼을 가리지 않는 작은 중앙 플로팅 토스트로 축소했습니다.

### 변경
- `입력 지우기`와 직접 입력 같은 보조 조작은 핵심 음성 CTA와 분리했습니다.
- PWA 캐시를 Home UX 0.4.2 기준으로 갱신했습니다.
