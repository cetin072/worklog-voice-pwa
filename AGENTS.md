# Worklog Voice PWA — 작업 기준

이 저장소는 실제 사용 중인 업무수첩 PWA의 원본 저장소다.

## 제품 정체성

업무수첩은 단순 음성 업무기록 앱이 아니라 **개인 업무에 필요한 여러 독립 도구를 한곳에서 실행하고, 필요할 때 업무기록·일정·CRM·브리핑으로 연결하는 개인 업무 통합 앱**이다.

제품 방향의 source of truth는 `PROJECT_CHARTER.md`, 모듈 경계의 상세 기준은 `docs/MODULE_ARCHITECTURE_V1.md`다.

빠르게 말하고 확실히 Notion에 저장하는 기존 업무기록 흐름은 계속 핵심 기능으로 보호한다.

## 모듈 개발 원칙

- **각 기능은 단독으로도 쓸 수 있는 수준으로 완성하고, 마지막에 업무수첩과 연결한다.**
- 새 기능은 가능한 한 `입력 → 자체 처리 → 자체 결과 → 저장/공유 → 선택적 업무 연결` 구조를 따른다.
- App Shell은 모듈을 실행·연결하는 얇은 허브를 지향하고, 각 모듈의 도메인 로직과 상태를 직접 소유하지 않는다.
- 모듈끼리 다른 화면 DOM, 전역 변수, 내부 저장구조를 직접 읽거나 수정하지 않는다.
- 모듈 간 연결은 명시적 이벤트, 함수 계약, 내부 표준 데이터 객체 또는 Adapter를 사용한다.
- Notion, Supabase, STT/AI 공급자, Kakao, Calendar 등 특정 외부 시스템의 스키마를 도메인 모듈에 직접 퍼뜨리지 않는다.
- 외부 연결은 Worklog/Notion, Schedule, CRM, Storage, STT, AI, Kakao 등의 Adapter / Service 경계를 사용한다.
- 외부 저장/연결이 실패해도 이미 생성된 사용자의 핵심 결과와 입력은 가능한 범위에서 잃지 않는다.
- 새 기능은 독립 검수 후 App Shell에 연결한다.
- 기존 정상 기능은 모듈화를 이유로 대규모 재작성하지 않고 수정·확장할 때 점진적으로 경계를 개선한다.

## 문서 로딩 경량화

- 일반 작업은 현재 Issue/요청 + `AGENTS.md` + 직접 수정할 인접 파일/테스트만 확인한다.
- 새 모듈 또는 모듈 경계 변경은 `PROJECT_CHARTER.md`와 `docs/MODULE_ARCHITECTURE_V1.md`를 추가로 확인한다.
- 큰 기능이나 여러 모듈 변경은 관련 planning/operations와 현재 PR/review를 추가로 확인한다.
- 아키텍처·보안·권한·배포 구조 변경에서만 중앙 공통 표준 원문을 반드시 다시 확인한다.
- 중앙 표준은 모든 작업의 상시 체크리스트가 아니라 아키텍처 판단과 감사의 상위 기준이다.

## 공통 웹 아키텍처 기준

- 웹 제작 공통 source of truth는 `cetin072/ai-development-system`의 `docs/WEB_ARCHITECTURE_STANDARD_V1.md`다.
- 핵심 원칙은 **Static by Default, Dynamic by Necessity**다.
- 이 앱은 동적 PWA이므로 JavaScript 사용 자체를 줄이는 것이 목표가 아니다.
- 앱을 열었을 때 핵심 녹음/입력/저장 진입점은 최초 App Shell에서 확정적으로 존재해야 한다.
- 별도 후처리 모듈이 실행되어야만 핵심 버튼이나 입력 UI가 생기는 구조를 사용하지 않는다.
- 음성 API, 네트워크, Notion 저장이 실패해도 사용자가 입력한 원문은 잃지 않아야 한다.
- loading / empty / error / retry 상태를 가능한 범위에서 명확히 구분한다.
- 비밀값과 중요한 검증은 클라이언트 JavaScript를 신뢰하지 않고 서버/Netlify 환경변수 등 신뢰 가능한 계층에서 처리한다.
- 기존 정상 동작 구조를 표준 준수만을 이유로 대규모 rewrite하지 않고 Issue #28에서 위험도 순으로 감사·정비한다.

## 데이터 역할

- 사용자 기기: 음성·사진 등 원본의 기본 위치.
- Notion: 현재 사람이 읽는 실제 업무기록의 기본 원장.
- 업무수첩 내부 표준 계약: 모듈 사이에서 공유하는 제품 데이터 기준.
- Supabase 등 앱 DB: 처리상태, 관계, 재시도, 사용량 등 내부 엔진 후보. 별도 확정 전 기존 다른 프로젝트 DB를 재사용한다고 가정하지 않는다.
- Netlify: PWA 배포, API/Function, 예약 실행과 외부 서비스 중계. 업무 원본의 장기 저장소로 보지 않는다.

Notion이 현재 업무기록의 기준이라는 사실과, 모듈 내부 코드가 Notion 스키마에 직접 종속되지 않아야 한다는 원칙을 함께 유지한다.

## 작업 시작 시

- 최신 `main`을 기준으로 현재 운영 상태를 확인한다.
- 기존 Issue / PR이 있으면 새로 시작하지 말고 해당 범위 안에서 이어간다.
- Notion DB 스키마와 Netlify 환경변수 이름을 임의로 바꾸지 않는다.
- 새 모듈은 독립 사용 흐름과 Result Model을 먼저 정의한 뒤 외부 Adapter를 연결한다.

## 브랜치 / PR

- 일반 패치는 기능 브랜치에서 작업한다.
- Draft PR로 검수한다.
- 사용자 승인 없이 main 병합 또는 production 구조 변경을 하지 않는다.

## 보안

다음 값은 코드, 커밋, Issue, PR, 로그 예시에 절대 넣지 않는다.

- `NOTION_TOKEN`
- `APP_ACCESS_KEY`
- 기타 개인 액세스 토큰

비밀값은 Netlify 환경변수로만 관리한다.

## 유지해야 할 v1 동작

- 모바일 우선 UI
- Web Speech API + 키보드 음성입력 폴백
- 원문 확인 후 저장
- 기존 Notion `🎙 업무 통합 기록` 데이터 소스 사용
- AI API 없이 동작하는 기본 업무기록 흐름
- 저장 실패 시 사용자가 원문을 잃지 않도록 한다.

## 변경 우선순위

1. 기록과 원본 보존의 신뢰성
2. 실제 업무에서 반복 사용 가능한 독립 기능 완성도
3. 입력 속도
4. 모바일 조작성
5. 보안·개인정보·비용 통제
6. 모듈 간 연결
7. 자동분류 정확도와 부가기능
