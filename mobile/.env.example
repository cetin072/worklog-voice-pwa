# Optional. The production API is used when this is omitted.
EXPO_PUBLIC_API_BASE_URL=https://worklog-voice-pwa.netlify.app

# QA-only build flag. Bypasses Android SpeechRecognizer but keeps real PCM,
# whisper.rn, canonical save, and briefing refresh paths.
EXPO_PUBLIC_FORCE_WHISPER_FALLBACK_QA=
